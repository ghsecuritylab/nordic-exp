#ifndef MOTOR_H
#define MOTOR_H

#include <stdint.h>

#include "defines.h"


class Motor {
private:

	//Side side;
	//bool enabled;

public:

	//Motor();
	//Motor(Side side);

	//void enable();
	//void disable();

	//void setSpeed(uint8_t speed);
	//void setDirection(Direction dir);
};

#endif // MOTOR_H